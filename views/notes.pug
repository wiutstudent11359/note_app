extends layout.pug 

block content
    div.container 
        h2 All Notes

        each note in notes 
            div.note-container
                a.note(href=`/notes/${ notes.id }`) #{note.subject}  
                a.delete(href='#') delete